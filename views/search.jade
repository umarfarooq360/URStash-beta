extends layout

block content
	h1 Searches
	-if (typeof(search) === 'undefined' ){
		//-do something
		p No results found
	-}else		
		ul
			each user, i in search
				li
					p #{user.Name} 
					p #{user.Author}
					br
					hr